list a = [4, 2, 3,] // throws error for not initializing w/ type "list"

//multi-statement for
node myNode = createNode()
for b in a { //comment
    c = b + 1 //comment
    //comment
    say("hello")

    int d //comment
} //comment

list y = [1,2,3,]
//empty for
for x in y{
}

list words = ["Shut", "the", "f**k", "up", "!",] //comment

//single-statement for
for word in words {
    say(word)
}
